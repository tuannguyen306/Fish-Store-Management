<div class="max-w-3xl mx-auto" style="padding: 32px 16px; background-color: white; border-radius: 8px; box-shadow: rgba(0, 0, 0, 0.1) 0px 4px 6px;">
  <h1 class="font-bold text-3xl mb-6 text-gray-800">New Sale</h1>
  
  <!-- Render the Sale Form -->
  <%= render 'form', sale: @sale %>
</div>
